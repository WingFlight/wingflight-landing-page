name: Deploy via SFTP

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      SFTP_HOST: ${{ secrets.SFTP_HOST }}
      SFTP_PORT: ${{ secrets.SFTP_PORT }}
      SFTP_USER: ${{ secrets.SFTP_USER }}
      SFTP_PASSWORD: ${{ secrets.SFTP_PASSWORD }}
      REMOTE_PATH: ${{ vars.SFTP_REMOTE_PATH || '.' }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: Validate required secrets
        run: |
          test -n "$SFTP_HOST"
          test -n "$SFTP_PORT"
          test -n "$SFTP_USER"
          test -n "$SFTP_PASSWORD"

      - name: Check SFTP connectivity
        run: |
          timeout 30s lftp <<EOF
          set cmd:fail-exit yes
          set net:timeout 10
          set net:max-retries 1
          set net:reconnect-interval-base 5
          set net:reconnect-interval-max 5
          set sftp:auto-confirm yes
          set ssl:verify-certificate no
          open -u "$SFTP_USER","$SFTP_PASSWORD" "sftp://$SFTP_HOST:$SFTP_PORT"
          cls "$REMOTE_PATH"
          bye
          EOF

      - name: Upload site
        run: |
          timeout 10m lftp -u "$SFTP_USER","$SFTP_PASSWORD" "sftp://$SFTP_HOST:$SFTP_PORT" <<EOF
          set cmd:fail-exit yes
          set net:timeout 10
          set net:max-retries 1
          set net:reconnect-interval-base 5
          set net:reconnect-interval-max 5
          set sftp:auto-confirm yes
          set ssl:verify-certificate no
          mirror --reverse --delete --verbose \
            --exclude-glob .git* \
            --exclude-glob .github \
            --exclude-glob node_modules \
            ./ "$REMOTE_PATH"
          bye
          EOF
